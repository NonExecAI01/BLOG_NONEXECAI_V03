# DISABLED — quota alerts run locally via aimodelpromptneai/scripts/install-quota-check-task.ps1
# Or manually: python notify_spend_limit.py --check

name: Cursor Quota Alert

on:
  workflow_dispatch:
    inputs:
      force:
        description: 'Rewrite alert even if already active this billing cycle'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  contents: write

jobs:
  quota-check:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled — use local quota check on Windows or run notify_spend_limit.py manually"
